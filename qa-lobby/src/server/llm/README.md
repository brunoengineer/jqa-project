This folder contains server-only LLM integrations.

- Ollama: local at `http://localhost:11434`
- OpenAI: uses `OPENAI_API_KEY` and optional `OPENAI_BASE_URL`

Entry point: `generateMarkdown` in `src/server/llm/index.ts`.
