# QA Lobby Prompts

This folder contains **default, versioned** prompt templates for each task.

- The app reads defaults from here.
- When you edit/save prompts in the UI, the saved versions are stored under `data/prompts/` and override these defaults.

Files are named by `taskId`.
